/*
	uniform float time;		// How long the app has been running for in seconds
	uniform mat4 objMat;	// 4x4 matrix containing the object's x, y, z, and t values
*/
const char * globalVertexShader = "uniform float time; uniform mat4 objMat;";